# KaySetu website

The full agent context for this repo lives in `AGENTS.md` — read it before
making changes. It covers the repo layout (work happens in
`KaySetu-Landing-Page-main/`), the Tailwind v4 token system, the
content-as-data architecture (`src/lib/content.ts`, `src/lib/modulePages.ts`),
how to verify UI changes without a test suite, known traps, and the current
state of in-flight work.

@AGENTS.md
